const DotSizeOptions = [2, 4, 6, 8, 10, 12];

// Preset ROI box sizes, in microns - a fixed list rather than a free-form
// field now that the box is placed (dragged into position) rather than
// resized by hand.
const RoiBoxSizeOptions = [100, 250, 500, 1000, 2000];

export { DotSizeOptions, RoiBoxSizeOptions };
