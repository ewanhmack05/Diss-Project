# annotation-store

Not built yet - next up. Will be the persistence backend for annotations,
replacing `image-viewer`'s current in-memory `AnnotationStoreContext`
(annotations currently live only in the browser tab's React state and are
lost on reload).
